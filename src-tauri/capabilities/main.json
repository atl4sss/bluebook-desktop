{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the local main window; no filesystem, shell, or remote-page permissions.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:window:allow-set-focus",
    "core:webview:allow-set-webview-focus"
  ]
}
